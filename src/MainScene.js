import Phaser, {
    Scene
} from 'phaser';

class MainScene extends Scene {
    constructor() {
        super({
            key: "MainScene"
        })
    }

    preload() {
        // TODO Add images, sprites, tilesets, audio files... here
    }

    create() {
        // TODO instantiate the game objects here
    }

    update() {
        // TODO a function called every delta
    }
}

export default MainScene